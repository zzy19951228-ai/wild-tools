# 006 · 岗位个人权重打分器（v0.1 已上线内测 → v0.2 立项）

> 来源：前同事真实痛点（2026-08-27 对话，原话存档于 01 业务对话日志）——"每个人在乎的点不一样，这是看机会最拿不住的点"。
> **内测地址：https://wild-tools.pages.dev/tools/006_岗位个人权重打分器/index.html**（手机电脑均可，微信直接点开）
> 生产序号 006；发布编号待周日选题定。当前状态：**内测中**，001 号内测用户（前同事）测试通过前不进发布流程。

## 版本阶梯（2026-08-28 立项，用户定调"要真自动，不要手动打分器"）

| 版本 | 形态 | 状态 |
|---|---|---|
| v0.1 | 手动权重 + 手动逐岗打分 | ✅ 已上线（打分引擎，后续版本复用） |
| **v0.2** | **粘贴 JD → LLM 自动解析（薪资/城市/硬性要求/红旗话术）→ 五维自动建议分 → 她微调 → 入列** | 🔨 本周建。**架构定调（2026-08-28）：BYOK 用户自带 key**——设置面板（提供商预设：DeepSeek推荐/智谱/通义/Kimi/OpenAI·Codex/自定义 → BaseURL 自动填 → key 密码框 → 模型下拉+手填 → 测试连接。**自定义为一等公民（2026-08-28 用户定调）：中转账号场景**——任意 OpenAI 兼容中转（one-api/new-api 类）直接填 BaseURL+key+模型名即用；**URL 智能归一化**：带不带 /v1、带不带完整路径都自动识别补全。内测用户自带 key 无门槛），key 仅存 localStorage；调用经 CF Functions 无状态代理纯转发（key 路过不落地+IP限流）；设置内嵌"3步开通DeepSeek key"图文引导；内测用户搞不定 key 时备选：单独限额临时 key（临时手段，主线 BYOK） |
| v0.2b | **薪资总览面板**：按城市聚合她收集的岗位 → 薪资区间分布图；新岗位薪资自动算分位 → 薪资维度默认分位制自动打（如样本 80 分位=5 分）| 🔨 与 v0.2 同期。数据三层：她的样本（主力，越用越准）+ LLM 市场基准（明标"模型估算"）+ 公开报告静态表（可选）。不爬虫 |
| v0.3 | 浏览器插件：BOSS 网页版一键给当前岗位打分（用户登录态、用户触发、不批量） | ⏸️ 等 v0.2 反馈；仅个人使用，不公开分发 |
| v0.4 | 定时推送匹配新岗位 | ❌ 无合法数据源，暂缓，不承诺 |

**明确不做**：爬取 BOSS/猎聘（无开放 API；违反协议且有巧达科技刑事判例先例；工具将公开分发，法律暴露不可接受）。

## 规格契约（tool-forge 第 2 步）

- **一个场景**：手上收藏了几个岗位，纠结哪个值得投——每个人的"值得"不一样
- **输入→输出**：设个人权重（5 维度滑杆 + 3 个预设档）→ 岗位逐维打分 1–5 → 加权排名 + 冠军拆解（赢在哪两个维度）
- **诚实边界**：分数用户自己打，工具只做结构化比较；本地 localStorage，不上传；"AI 辅助开发"，不假装自动评估岗位
- **演示时刻**：切换预设档（方向/钱/稳），第一名当场换人——"同一份列表，三个人选出三个答案"

## 三个预设档 = 内置的用户画像（化名，已脱敏）

方向优先（她）/ 钱优先（"春春"）/ 稳优先（"阿顾"）——档位名不用真名，内容红线执行。

## 001 号内测用户测试清单（给前同事，收集原话）

1. 五个维度够用吗？你最在乎的在里面吗？缺哪个？
2. 加一个岗位到打完分，超过 5 分钟了吗？（超了 = 太烦）
3. 算出来的排名，你服不服？不服的话——是权重没设对，还是工具没抓住你的纠结？
4. 有没有哪一刻你想"要是能 ___ 就好了"？（这句原话最值钱）

## 发布包（待内测通过后走四锻造）

- 钩子候选：「同一份岗位列表，三个人选出三个答案」（数字钩子+反差）／「投简历前，先让它替你纠结 5 分钟」
- ⚠️ 红线：裁员题材共情不消费；故事与三画像全部化名；发布前需她同意使用其故事框架（脱敏后）
